  import 'package:flutter/material.dart';

  // A calming, patient-friendly color palette for the app.
  const ColorScheme customColorScheme = ColorScheme.light(
    primary: Color(0xFF1976D2), // A deep, trustworthy blue
    secondary: Color(0xFF4CAF50), // A friendly, supportive green
    surface: Color(0xFFF5F5F5), // A soft, light gray for the background
    error: Color(0xFFD32F2F), // A standard red for errors
    onPrimary: Colors.white,
    onSecondary: Colors.white,
    onSurface: Color(0xFF212121),
  );
